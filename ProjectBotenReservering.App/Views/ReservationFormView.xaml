<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cal="clr-namespace:Plugin.Maui.Calendar.Controls;assembly=Plugin.Maui.Calendar"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:vm="clr-namespace:ProjectBotenReservering.App.ViewModels"
             xmlns:models="clr-namespace:ProjectBotenReservering.Core.Models;assembly=ProjectBotenReservering.Core"
             x:Class="ProjectBotenReservering.App.Views.ReservationFormView"
             x:DataType="vm:ReservationFormViewModel"
             Shell.NavBarIsVisible="False">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior EventName="Appearing" 
                                        Command="{Binding LoadReservationsCommand}" />
    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="CalYearLabelStyle" TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource Primary}" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
            </Style>
            <Style x:Key="CalMonthLabelStyle" TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource Primary}" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
            </Style>
            <Style x:Key="CalDayLabelStyle" TargetType="Label">
                <Setter Property="TextColor" Value="Black" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="VerticalTextAlignment" Value="Center" />
            </Style>
            <Style x:Key="CalSelectedDateLabelStyle" TargetType="Label">
                <Setter Property="TextColor" Value="White" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="VerticalTextAlignment" Value="Center" />
            </Style>
            <Style x:Key="CalWeekdayTitleStyle" TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource Primary}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto, Auto, Auto, Auto" Padding="10" RowSpacing="10">

        <!-- Header -->
        <Grid Grid.Row="0" ColumnDefinitions="Auto, *" Margin="0,0,0,10">
            <Button Grid.Column="0" Text="Terug" Command="{Binding BackCommand}" BackgroundColor="LightGray" TextColor="Black" HeightRequest="36" HorizontalOptions="Start" />

            <Label Grid.Column="1" 
                   Text="{Binding CurrentBoatType.Name, TargetNullValue=''}" 
                   FontSize="18" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center" 
                   VerticalOptions="Center" />
        </Grid>

        <!-- Calendar & Reservation List Area -->
        <Grid Grid.Row="1" 
              ColumnDefinitions="Auto, 300" 
              ColumnSpacing="10" 
              HorizontalOptions="Center">

            <Border Grid.Column="0" 
                    Stroke="{StaticResource Primary}"
                    StrokeThickness="4"
                    StrokeShape="RoundRectangle 20,0,0,20"
                    Background="{StaticResource Primary}"
                    Padding="1,1">
                <VerticalStackLayout Spacing="5" BackgroundColor="{StaticResource Secondary}">
                    <cal:Calendar 
                        SelectedDate="{Binding SelectedDate}"
                        MinimumDate="{Binding MinDate}"
                        YearLabelStyle="{StaticResource CalYearLabelStyle}"
                        MonthLabelStyle="{StaticResource CalMonthLabelStyle}"
                        DaysLabelStyle="{StaticResource CalDayLabelStyle}"
                        SelectedDateLabelStyle="{StaticResource CalSelectedDateLabelStyle}"
                        DaysTitleLabelStyle="{StaticResource CalWeekdayTitleStyle}"
                        SelectedDayBackgroundColor="{StaticResource Primary}"
                        TodayOutlineColor="{StaticResource Primary}"
                        Events="{Binding Events}"
                        EventsScrollViewVisible="False"
                        EventIndicatorColor="Blue"
                        EventIndicatorSelectedColor="LightBlue"
                        VerticalOptions="Fill"
                        SwipeUpToHideEnabled="False"
                        FooterSectionVisible="False"
                        HeightRequest="420" 
                        WidthRequest="700"
                        FirstDayOfWeek="Monday">

                        <cal:Calendar.EventTemplate>
                            <DataTemplate>
                                <StackLayout>
                                    <Label Text="{Binding clientId}" />
                                    <Label Text="{Binding BoatId}" />
                                </StackLayout>
                            </DataTemplate>
                        </cal:Calendar.EventTemplate>
                    </cal:Calendar>

                    <Label Text="{Binding DateWarningText}" 
                           IsVisible="{Binding HasDateWarning}"
                           TextColor="Red"
                           FontSize="12"
                           FontAttributes="Bold"
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Border>

            <Border Grid.Column="1"
                    Stroke="{StaticResource Primary}"
                    StrokeThickness="2"
                    StrokeShape="RoundRectangle 10"
                    BackgroundColor="White"
                    Padding="10"
                    VerticalOptions="Fill">
                <Grid RowDefinitions="Auto, *">
                    <Label Grid.Row="0" 
                           Text="Huidige reserveringen" 
                           FontSize="16" 
                           FontAttributes="Bold" 
                           TextColor="{StaticResource Primary}" 
                           Margin="0,0,0,10"/>

                    <CollectionView Grid.Row="1" 
                                    ItemsSource="{Binding Reservations, Mode=TwoWay}"
                                    EmptyView="Nog geen reserveringen">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:Reservation">
                                <Frame Margin="0,0,0,8" Padding="10" BorderColor="LightGray" CornerRadius="5" HasShadow="False">
                                    <VerticalStackLayout Spacing="2">
                                        <HorizontalStackLayout Spacing="5">
                                            <Label Text="{Binding StartTime, StringFormat='{0:HH:mm}'}" FontAttributes="Bold" TextColor="Black"/>
                                            <Label Text="-" TextColor="Black"/>
                                            <Label Text="{Binding EndTime, StringFormat='{0:HH:mm}'}" FontAttributes="Bold" TextColor="Black"/>
                                        </HorizontalStackLayout>

                                        <Label Text="{Binding ClientId, StringFormat='Client ID: {0}'}" FontSize="12" TextColor="Gray"/>
                                        <Label Text="{Binding BoatId, StringFormat='Boat ID: {0}'}" FontSize="12" TextColor="Gray"/>
                                    </VerticalStackLayout>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2" ColumnDefinitions="*, *" ColumnSpacing="40" Margin="20,10,20,0">

            <VerticalStackLayout Grid.Column="0" Spacing="15" VerticalOptions="Start">
                <Label Text="Tijd" FontSize="18" FontAttributes="Bold" TextColor="{StaticResource Primary}" />
                <VerticalStackLayout>
                    <Label Text="Starttijd:" FontSize="12" TextColor="Gray"/>
                    <TimePicker Format="HH:mm" Time="{Binding StartTime}" />
                </VerticalStackLayout>
                <VerticalStackLayout>
                    <Label Text="Eindtijd:" FontSize="12" TextColor="Gray"/>
                    <TimePicker Format="HH:mm" Time="{Binding EndTime}" />
                </VerticalStackLayout>
                <Label Text="{Binding TimeWarningText}" 
                       IsVisible="{Binding HasTimeWarning}"
                       TextColor="Red"
                       FontSize="12"
                       FontAttributes="Bold" />
            </VerticalStackLayout>

            <VerticalStackLayout Grid.Column="1" Spacing="10" VerticalOptions="Start">
                <Grid ColumnDefinitions="*, Auto">
                    <Label Grid.Column="0" Text="Roeiers" FontSize="18" FontAttributes="Bold" TextColor="{StaticResource Primary}" />
                    <Label Grid.Column="1" Text="{Binding SeatStatusText}" FontSize="18" FontAttributes="Bold" />
                </Grid>
                <Picker Title="Voeg roeier toe" ItemsSource="{Binding AvailableClients}" ItemDisplayBinding="{Binding FullName}" SelectedItem="{Binding SelectedClientToAdd}" BackgroundColor="#f0f0f0" IsVisible="{Binding IsPickerSupported}" />

                <VerticalStackLayout IsVisible="{Binding IsMacCatalyst}" Spacing="6">
                    <Label Text="Selecteer roeier:" FontSize="12" TextColor="Gray" />
                    <CollectionView ItemsSource="{Binding AvailableClients}" SelectionMode="None" HeightRequest="180">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:Client">
                                <Frame Padding="8" Margin="0,4" HasShadow="False" BorderColor="#e0e0e0" CornerRadius="6" BackgroundColor="#ffffff">
                                    <Grid ColumnDefinitions="*, Auto" VerticalOptions="Center">
                                        <Label Grid.Column="0" Text="{Binding FullName}" VerticalOptions="Center" FontSize="14" />
                                        <Button Grid.Column="1" Text="Voeg toe" Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ReservationFormViewModel}}, Path=AddClientCommand}" CommandParameter="{Binding .}" HeightRequest="30" Padding="10,0" />
                                    </Grid>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>

                <Picker Title="Roeiers toevoegen" ItemsSource="{Binding AvailableClients}" ItemDisplayBinding="{Binding FullName}" SelectedItem="{Binding SelectedClientToAdd}" BackgroundColor="#f0f0f0" />

                <CollectionView ItemsSource="{Binding SelectedClients}" SelectionMode="None" HorizontalOptions="FillAndExpand" HeightRequest="180">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical" Span="2" HorizontalItemSpacing="6" VerticalItemSpacing="6" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Client">
                            <Frame Padding="8" Margin="6" HasShadow="False" BorderColor="#e0e0e0" CornerRadius="6" BackgroundColor="#ffffff" HorizontalOptions="FillAndExpand">
                                <Grid ColumnDefinitions="*, Auto, Auto" VerticalOptions="Center" HorizontalOptions="FillAndExpand">
                                    <Label Grid.Column="0" Text="{Binding FullName}" VerticalOptions="Center" FontSize="14" LineBreakMode="WordWrap" HorizontalOptions="StartAndExpand" />

                                    <Button Grid.Column="1" Text="!" TextColor="Red" FontAttributes="Bold" FontSize="18" VerticalOptions="Center" Margin="8,0,8,0"
                                            BackgroundColor="Transparent"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ReservationFormViewModel}}, Path=ShowQualificationWarningCommand}"
                                            CommandParameter="{Binding .}"
                                            AutomationProperties.HelpText="{Binding QualificationHelpText}"
                                            IsVisible="{Binding IsUnderqualified}" />

                                    <Button Grid.Column="2" Text="X" TextColor="Red" BackgroundColor="Transparent" Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ReservationFormViewModel}}, Path=RemoveClientCommand}" CommandParameter="{Binding .}" HeightRequest="28" WidthRequest="28" Padding="0" HorizontalOptions="End"/>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </Grid>

        <Button Grid.Row="3" Text="Reserveren" Command="{Binding SaveReservationCommand}" Margin="0,20,0,0" FontSize="18" FontAttributes="Bold" VerticalOptions="End" />
    </Grid>
</ContentPage>